// Define the module for constants used throughout the application
pub mod constants;

// Define the module for custom error types and error handling
pub mod error;

// Define the module for network-related functionality, including parsing network and key types
pub mod network;

// Define the module for address-related functionality, including address derivation and info retrieval
pub mod address;

// Define the module for HTTP client setup and configuration
pub mod client;

// Define the module for CLI argument parsing and handling
pub mod cli;
